name: Run from external cron

on:
  repository_dispatch:
    types: [run-from-cron]

jobs:
  run-it:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Say Hello
        run: echo "Hi, this was triggered from cron-job.org!"
